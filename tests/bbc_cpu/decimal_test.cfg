MEMORY {
    RAM: start = $0000, size = $10000, type = rw, fill = yes, fillval = $FF, file = %O;
}
SEGMENTS {
    ZEROPAGE: load = RAM, type = rw;
    DATA:     load = RAM, type = rw, start = $0200;
    CODE:     load = RAM, type = rw, start = $0400;
}
