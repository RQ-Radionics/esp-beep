MEMORY {
    RAM:     start = $0000, size = $FFFA, type = rw, fill = yes, fillval = $ff, file = %O;
    VECTORS: start = $FFFA, size = $0006, type = ro, fill = yes, fillval = $ff, file = %O;
}
SEGMENTS {
    ZEROPAGE: load = RAM, type = rw, start = $000A;
    DATA:     load = RAM, type = rw, start = $0200;
    CODE:     load = RAM, type = rw, start = $0400;
    VECTORS:  load = VECTORS, type = ro;
}
